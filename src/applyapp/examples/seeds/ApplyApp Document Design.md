# ApplyApp Document Design

Example pointer. The format step also reads the built-in design spec in the ApplyApp source. Keep this filename so the file is classified as the design seed. Edit the spec in `src/applyapp/design.py` to change type, margins, and the greeting rule.
